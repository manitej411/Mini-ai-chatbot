# Mini-ai-chatbot
1. User enters a message in the web interface 2. Frontend sends a POST request to the Flask backend 3. Backend forwards the prompt to the LLaMA 3 model via Ollama 4. The model processes the input using Transformer components (attention, embeddings, etc.) 5. Generated response is sent back to the frontend and displayed
